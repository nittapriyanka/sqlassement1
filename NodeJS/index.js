/*const Logger = require('./logger');

const log = new Logger();
log.on('message',(data)=> console.log('called listener',data));

log.log('Welcome to NodeJs');*/


const lan= require('./javafile')
console.log(lan.name);