1)SELECT DNAME
FROM DEPT
WHERE DEPTNO IS NULL

2)
SELECT ENAME,JOB
  FROM EMP
  WHERE MGR IS NULL

3)
SELECT E.ENAME,E.JOB,E.MGR, D.LOC, E.COMM
FROM EMP AS E
INNER JOIN 
DEPT AS D
ON E.DEPTNO=D.DEPTNO
WHERE D.LOC IN('NEWYORK')  AND E.COMM >1000

4)
SELECT COUNT(ENAME) AS 'COUNTOFEMPLOYEES'
FROM EMP
WHERE DEPTNO IN 
             (SELECT DEPTNO
              FROM DEPT
              WHERE LOC = 'CHICAGO')

5)
SELECT ENAME 
FROM EMP
WHERE DEPTNO IN 
             (SELECT DEPTNO
              FROM DEPT
              WHERE LOC = 'CHICAGO')

6)
SELECT ENAME, LOC
FROM
EMP AS E
INNER JOIN 
DEPT AS D
ON
E.DEPTNO = D.DEPTNO
ORDER BY LOC

7)
SELECT E.*
FROM EMP E LEFT JOIN EMP E1
ON.E.MGR=E1.EMPNO
WHERE E.HIREDATE<E1.HIREDATE

8)
SELECT ENAME 
FROM EMP AS E
WHERE HIREDATE < (SELECT HIREDATE FROM EMP WHERE EMPNO=E.MGR)

9)
SELECT COUNT(DISTINCT MGR) AS 'COUNT'
FROM EMP

10)
SELECT ENAME,JOB
FROM EMP
WHERE MGR IS NULL

11)
SELECT ENAME,DEPTNO
FROM EMP
WHERE ENAME IN ('SMITH')

12)
SELECT E.ENAME,E.JOB,E.MGR, D.LOC
FROM EMP AS E
INNER JOIN 
DEPT AS D
ON E.DEPTNO=D.DEPTNO
WHERE D.LOC NOT IN ('BOSTON') AND E.JOB IN ('SALESMAN')

13)
SELECT E.EMPNO,E.ENAME,E.JOB,E.MGR,E.HIREDATE,E.SAL,E.COMM,E.DEPTNO, D.LOC
FROM EMP AS E
INNER JOIN 
DEPT AS D
ON E.DEPTNO=D.DEPTNO

14)
SELECT ENAME,JOB
FROM EMP
WHERE JOB IN ('SALESMAN') AND SAL<(SELECT SAL FROM EMP WHERE ENAME='MILLER')

15)
SELECT E.EMPNO,E.ENAME,E.JOB,E.MGR,E.HIREDATE,E.SAL,E.COMM,E.DEPTNO, D.LOC
FROM EMP AS E
INNER JOIN 
DEPT AS D
ON E.DEPTNO=D.DEPTNO
WHERE D.LOC='CHICAGO'

16)
SELECT ENAME
FROM EMP
WHERE SAL > (SELECT MIN(SAL) FROM EMP WHERE JOB='MANAGER')

17)
SELECT ENAME,D.DEPTNO
FROM EMP AS E
INNER JOIN DEPT AS D
ON E.DEPTNO=D.DEPTNO
WHERE D.DEPTNO = '10'

18)
SELECT ENAME
FROM EMP
WHERE JOB IN ('MANAGER')

19)
SELECT DNAME
CASE WHEN DNAME!='SALES' THEN 'NOT SALE'
ELSE 'SALE'
END AS CASE1 FROM DEPT

20)
SELECT SAL,CASE WHEN SAL>1000 THEN 'GREATER THAN 1000'
ELSE '1000' END AS 'COMPARISION'
FROM EMP

21)
SELECT SAL,CASE WHEN SAL < 1000 THEN 'LOW'
WHEN SAL>=1000 AND SAL<=2500 THEN 'MEDIUM'
ELSE 'HIGH' END AS 'COMPARSION'
FROM EMP